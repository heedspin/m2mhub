<% semantic_form_for :search, @search_item, :url => items_path, :html => {:method => :get} do |f| %>
	<% f.inputs do %>
	  <%= f.input :fpartno %>
	<% end %>
  <% f.buttons do %>
	  <%= f.commit_button 'Search' %>
	<% end %>
<% end %>

<% if @items %>
<%= will_paginate @items %>
<%= render 'list' %>
<% end %>